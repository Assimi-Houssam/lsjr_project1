# LocalQuizHub — environment template for electron_app
# Copy this file to .env and edit values for your environment.

# Port that the embedded HTTP server will listen on (phones connect here)
LOCAL_HTTP_PORT=3000

# Bind host for the HTTP server. 0.0.0.0 makes the server reachable on the LAN
# (do NOT change this to 127.0.0.1 if you want phones to connect)
HOST=0.0.0.0

# If you want Electron to spawn the quiz Vite dev server automatically in dev, set START_VITE=1
# In production this should be 0 (do NOT run Vite in production)
START_VITE=0

# Vite dev server port for the quiz app (only used if START_VITE=1)
VITE_PORT=5173

# If you run the renderer dev server separately, you can set ELECTRON_RENDERER_URL so the BrowserWindow loads it
# e.g. http://192.168.0.10:5173
ELECTRON_RENDERER_URL=


# Control flags for local development
# NODE_ENV=development
DEV=1
NODE_ENV=production

# SQLite / Prisma database URL. For dev you can use a relative sqlite file.
# In production it's recommended to point to an absolute path inside the app's userData folder.
# Example: file:./dev.db  or file:/absolute/path/to/dev.db
DATABASE_URL=file:./dev.db

# # Remote server sync (optional): URL where the Electron app should push batched data
# REMOTE_SYNC_URL=
# REMOTE_SYNC_TOKEN=

# Logging level (info|debug|warn|error)
LOG_LEVEL=info


